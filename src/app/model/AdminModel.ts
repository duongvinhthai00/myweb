export interface AdminModel{
    id ?: number,
    name : string,
    email : string,
    phone :string,
    avatar : string,
    active ?: number,
    password :string
    created_at ?: Date,
    update_at ?: Date,
    user_name : string,
    address : string
}